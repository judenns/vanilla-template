# ==========================================
# Environment Variables - Vanilla JS Template
# ==========================================
#
# IMPORTANT: Only variables prefixed with VITE_ are exposed to client-side code
#
# Usage in code:
#   console.log(import.meta.env.VITE_APP_TITLE)
#   console.log(import.meta.env.VITE_API_URL)
#
# Built-in variables:
#   import.meta.env.MODE        - 'development' or 'production'
#   import.meta.env.DEV         - true in dev mode
#   import.meta.env.PROD        - true in production mode
#
# ==========================================

# App Configuration
VITE_APP_TITLE=Vanilla JS Template
VITE_APP_ENV=development

# API Configuration
VITE_API_URL=http://localhost:3000

# ==========================================
# For Production Deployment:
# ==========================================
# Don't edit this file for production!
# Instead, set environment variables in your hosting platform:
#
# Vercel:
#   Project Settings → Environment Variables
#
# Netlify:
#   Site Settings → Build & deploy → Environment
#
# Example production values:
#   VITE_API_URL=https://api.yourapp.com
#   VITE_APP_ENV=production
#
# ==========================================
# For Local Secrets (API Keys, Tokens):
# ==========================================
# Create a .env.local file (already in .gitignore)
# Example .env.local:
#   VITE_API_KEY=your-secret-key
#   VITE_STRIPE_KEY=pk_test_xxxxx
#
# .env.local will override values in this file
# and will NOT be committed to git
# ==========================================
